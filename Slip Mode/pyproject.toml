# pyproject.toml
[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "slip-mode"
version = "0.1.0"
description = "Digital Evidence Platform - Slip Mode"
requires-python = ">=3.9"
dependencies = ["pydantic", "toml", "typing-extensions"]
