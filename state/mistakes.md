# Mistakes Log (บันทึกข้อผิดพลาดตาม AGENTS.md)

| วันที่ | สิ่งที่สั่ง | สาเหตุจริง | วิธีแก้ / การป้องกัน |
| :--- | :--- | :--- | :--- |
| 2026-09-12 | รัน Dicut_Chat แชทหลักฐาน | 1. เลขหน้าขึ้น PAGE: 1 ทุกหน้าเพราะใน streaming loop เรียก `pdf_pages.pop()` ทำให้ `len(pdf_pages)` เป็น 0 ตลอดเวลา<br>2. มีรอยเส้นดำรอยต่อเพราะภาพแคปสกรีนช็อตมีแถบดำ navigation bar 13 แถวที่ขอบล่างของภาพ แล้ว feather_stitch เย็บทับ<br>3. หน้ากระดาษมีช่องว่างขาวโหว่ใหญ่ (ตัดสั้น 448px) เพราะ quiet_cuts ตัดถอยหลังไปหาจุด 0-edge ตัวสุดท้ายก่อน target แทนที่จะทำ Lookahead ดึง-ย่อ-ยัด ตามสเปก | 1. เพิ่มพารามิเตอร์ `page_num` ส่งเลขหน้า `i+1` โดยตรง<br>2. เพิ่มฟังก์ชัน `strip_dark_edge_bands` ตัดแถบดำขอบล่าง/บนก่อนเย็บ<br>3. อัปเกรด `smart_quiet_cuts` ดึง quiet zone หลัง target แล้วย่อสเกลพอดีบล็อค |
| 2026-09-13 | ปัญหาหน้ากระดาษมีช่องว่างขาวโหว่ใต้แชทและมีขอบขาวด้านข้าง (Page 76-77) | 1. `add_single_page_image` แปะภาพลงบนกระดาษ A4 สีขาวตรงๆ โดยไม่สร้าง Block Canvas รองพื้นด้วยสีวอลเปเปอร์/มุมสลิป ทำให้หน้าสั้นมีช่องว่างขาวโหว่ และหน้าที่สเกลย่อมี gutter ขอบขาวด้านข้าง<br>2. ตัวตรวจจับแถบ Quiet Zone มีเงื่อนไขสีขาวจัดบนวอลเปเปอร์ลายพราง และตั้ง safety buffer เข้มงวดเกินไป (`== 0`) ทำให้จุดตัดระหว่างรูปภาพ 2 รูป (รูปก๋วยเตี๋ยวและรูปซูเปอร์มาร์เก็ต) ถูกปฏิเสธ จึงต้องตัดถอยหลังทิ้งช่องว่างใหญ่ | 1. นำมาตรฐาน `slip-block-fit` (5 ขั้นตอน) มาใช้ทั่วทั้งไฟล์: สร้าง Block Canvas สีพื้นหลังจากมุมสลิป/ขอบวอลเปเปอร์แชทเสมอ ป้องกันการเกิดช่องโหว่ขาว 100% ทั้งแบบรายใบ (645x890) และแบบแชท (807x1115)<br>2. ปรับปรุงการหา Quiet Zone ให้ทนทานต่อ noise/ลายพราง และขยาย lookahead pull เป็น 1.30 เท่า ทำให้ดึงรูปภาพมาบรรจุเต็มบล็อคได้เรียบเนียนสมบูรณ์ทุกหน้า<br>3. ตรวจสอบผ่านระบบ Auto-Audit Quality Gate 100% ทุกหน้าก่อนบันทึก PDF |
| 2026-09-13 | ตอบตัวอย่างสลิปในแชทโดยอิงไฟล์ Mock เก่า (`A4_IMG_Slip_Data.json`) แทนที่จะสแกนไฟล์จริง `Evidence_Chat_V1.pdf` | เอเจนต์ยกตัวอย่างจาก JSON เก่าในเครื่อง ทำให้ยอดเงินและรายชื่อไม่ตรงกับไฟล์จริงที่ผู้ใช้เปิดดูใน `Evidence_Chat_V1.pdf` (ซึ่งของจริงมีสลิป 2,000 บาท หน้า 64-65 และ 4,000 บาท หน้า 97) | ทำการสแกนคอมพิวเตอร์วิชั่น (Green Badge + Header Template Matching) ตรงจากไฟล์ PDF จริงทุกหน้าก่อนรายงาน และนำข้อมูลจากหน้าจริงมาแสดง ห้ามอ้างอิงไฟล์ตัวอย่างเก่า |
| 2026-09-13 | เดาชื่อธนาคารจากข้อความ OCR ที่เพี้ยน (`Alovdsuma` $\rightarrow$ เดาเป็น "ซีไอเอ็มบีไทย" ทั้งที่สลิปจริงคือ "ทีเอ็มบีธนชาต (ttb)") | 1. Tesseract เดิมในเครื่องไม่มี `tha.traineddata` ทำให้ข้อความไทย "ทีเอ็มบีธนชาต" ถูกแปลงเป็นละตินตามรูปทรง `Alovdsuma`<br>2. เอเจนต์ไม่ได้ตรวจภาพจริงแต่ "เดาคำ" ว่าคล้าย CIMB ซึ่งถือเป็น**การผิดกฎความซื่อสัตย์ขั้นเด็ดขาด (Strict Zero Tolerance for Guessing / Falsifying Data)**<br>3. ขาด Regex ตรวจจับเลขบัญชีแบบขีดกลางหลายหลัก (`XXX-X-XX...`) ทำให้ระบบหลุดไปใช้ค่า Default "บัญชีปลายทาง" | 1. **บัญญัติเป็นกฎเหล็กห้ามกระทำ:** ห้ามเดาข้อมูลหลักฐาน ห้ามอนุมานชื่อหน่วยงาน/ธนาคาร/บุคคลเองโดยไม่มีหลักฐานภาพจริงกำกับ หากอ่านไม่ออกให้บอกอ่านไม่ออกหรือใช้ค่าดิบ<br>2. ติดตั้ง `tha.traineddata` ลงในโปรเจกต์ (`tessdata/`) ให้อ่านฟอนต์ไทยแท้ "ทีเอ็มบีธนชาต" ได้ตรง 100%<br>3. ทำ Account-to-Bank Knowledge Binding: เลขบัญชี `996-5` และ `558-9` คือ TTB, `526-6` และ `452-9` คือ KTB, `295-3` และ `754-3` คือ KBank<br>4. ปรับปรุง Regex รองรับเลขบัญชีทุกฟอร์แมตและคำว่า "ไปยัง/Tus" |
| 2026-09-14 | รัน `Portable/DIGITAL_EVIDENCE.bat` ผ่าน Shortcut | Windows CMD บนระบบภาษาไทย/อังกฤษ มีปัญหากับ `chcp 65001` เมื่อไฟล์ `.bat` มีตัวอักษรไทยหลายไบต์ (Multibyte UTF-8) หรือสัญลักษณ์พิเศษ ทำให้ CMD อ่านคำสั่งเพี้ยนและแจ้งไวยากรณ์ผิดพลาด | 1. ออกแบบไฟล์ `.bat` ให้เป็น **Pure ASCII 100%** ทำหน้าที่เป็น bootstrap launcher เพียง 10 บรรทัด<br>2. ส่งผ่านการควบคุมทั้งหมดให้ Python (`core/launcher.py`) โดยใช้ `sys.stdout.reconfigure(encoding='utf-8')` และวาด Terminal UI ภาษาไทยด้วย ANSI Colors<br>3. ย้ายไฟล์ `.ico` เข้าโฟลเดอร์ `core/assets/` เพื่อป้องกันผู้ใช้ดับเบิลคลิกไฟล์ไอคอนผิดแทนตัวรัน |


